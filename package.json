{
  "name": "server",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bunx prisma migrate deploy && bun run ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bunx prisma generate && bun --watch run ./src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/multer": "^2.0.0",
    "prisma": "^6.11.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@prisma/client": "^6.11.0",
    "better-auth": "^1.2.12",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "got": "^14.4.7",
    "multer": "^2.0.1",
    "node-firebird": "^1.1.9",
    "zod": "^3.25.67"
  }
}
